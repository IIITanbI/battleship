<Window x:Class="BattleshipUI.New_Game.NewGame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BattleshipUI.New_Game"
        mc:Ignorable="d"
        Title="NewGame" Height="300" Width="300">
    <StackPanel>
        <TextBlock Text="Enter size of battlegorund" />
        <TextBox Name="ResponseTextBox" TextChanged="ResponseTextBox_TextChanged"/>
        <Grid Name="MainGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Label Content="Skin" Grid.Row="0" Grid.Column="0"></Label>
            <Label Content="Count" Grid.Row="0" Grid.Column="1"></Label>
        </Grid>
        <Button Name="OkButton" Content="OK" Click="Button_Click" />
    </StackPanel>
</Window>
